# What the committed check scripts actually import (issue #24; previously #22).
# Pure-software arbitrary precision => bit-for-bit reproducible outputs (issue #20).
# Exploratory extras live in requirements-dev.txt and are NOT needed to verify results.
mpmath==1.3.0

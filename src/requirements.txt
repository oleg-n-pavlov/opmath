# Pinned to the exact versions of the working environment (issue #22).
# The check scripts in src/ depend only on mpmath (pure-software arbitrary precision,
# hence bit-for-bit reproducible outputs, issue #20); the remaining packages are pinned
# as the research environment used for exploratory numerics.
mpmath==1.3.0
numpy==2.5.2
scipy==1.18.0
sympy==1.14.0
matplotlib==3.11.1
